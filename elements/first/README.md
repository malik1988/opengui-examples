# How to use

```bash

git submodule update --init --recursive
# or manual clone the fltk source to elements
# git clone https://github.com/cycfi/elements  elements

mkdir build && cd build
cmake build ..

```
