# How to use

```bash
# if fltk-1.4.x not exists
git submodule update --init --recursive
# or manual clone the fltk source to fltk-1.4.x
# git clone https://github.com/fltk/fltk.git  fltk-1.4.x

mkdir build && cd build
cmake build ..

```
